spring:
  application:
    name: gateway
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: local
      config:
        server-addr: localhost:8848
        namespace: local
server:
  port: 18080
